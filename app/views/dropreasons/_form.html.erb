<%= form_for(@dropreason) do |f| %>
  <% if @dropreason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dropreason.errors.count, "error") %> prohibited this dropreason from being saved:</h2>

      <ul>
      <% @dropreason.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
